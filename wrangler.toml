# GeneGuessr API Worker
#
# DATA FLOW (so future-you doesn't get confused):
# ------------------------------------------------
# 1. Protein data source: tools/thoteins/data/geneguessr/proteins.json
# 2. Upload to D1: python tools/thoteins/scripts/upload_local_database.py --remote
# 3. This worker serves data from D1 via /api/protein, /api/game/bootstrap, etc.
# 4. Frontend (quartz/static/geneguessr/app.js) consumes the API
#
# Worker deployment: GitHub Actions runs `wrangler deploy` on push to main
# D1 database update: Manual - run upload_local_database.py --remote after
#                     updating proteins.json

name = "geneguessr-api"
main = "workers/index.js"
compatibility_date = "2025-11-12"

# Custom domain routes
routes = [
  { pattern = "geneguessr.brinedew.bio/*", zone_name = "brinedew.bio" }
]

[[d1_databases]]
binding = "DB"
database_name = "geneguessr"
database_id = "c213abba-394b-42e0-ba15-a1b179cbe7db"

[[kv_namespaces]]
binding = "KV"
id = "7d59f063885e415ab3df5301393a114d"

[[durable_objects.bindings]]
name = "GAME_SESSIONS"
class_name = "GameSession"
script_name = "geneguessr-api"

[[migrations]]
tag = "v1"
new_sqlite_classes = ["GameSession"]

[[r2_buckets]]
binding = "STRUCTURES_BUCKET"
bucket_name = "geneguessr-structures"
