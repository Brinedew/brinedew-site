[project]
name = "iconoplasm-extension-e2e"
version = "0.1.0"
requires-python = ">=3.12,<3.13"
dependencies = [
  "pytest==8.4.2",
  "pytest-timeout==2.4.0",
  "selenium==4.35.0",
]

[tool.pytest.ini_options]
addopts = "-q --strict-markers --timeout=60"
testpaths = ["tests"]
