# B-742 recovery handoff — 9 September 2026

## Recovery ownership fence: RECOVERY-001

Read [Recovery Ownership Contract](RECOVERY_OWNERSHIP_CONTRACT.md) before incident, capacity,
deployment or recovery work. The accepting executor owns delivery through a
verified user operation; the owner does not run deployment/SQL commands or
time resets. A safeguard must preserve a tested compatible repair path
under its exact failure. Continue non-deploying source publication and
isolated tests while live D1 is exhausted. Refusal is containment evidence.
Deferred work needs an inspected registered executor, accessible source,
wake condition and failure destination. A chat or Linear edit does not
schedule it. Preserve security, actual admission and schema compatibility
when replacing a deadlocking guard through explicit change control.
This contract adds no new production gate or prerequisite redesign.
Historical checkpoints below require fresh state reconciliation.

The owner requested a budget-conscious handoff to a cheaper model. **B-742 is
not resolved.** Finish the work below without repeating the completed incident
investigation. This is a dated checkpoint: verify checkout and installed state
before any release or restart.

## Start here

Read the workspace/project AGENTS files, `D1_READ_EXHAUSTION_PREVENTION.md`,
`CLOUDFLARE_SYSTEM_BUDGET_AUDIT.md`, the relevant registered architecture fences,
and the current Linear B-742 description/comments. The issue contains the full
acceptance contract. The workstation companion is
`D:\Coding\Iconoplasm\docs\B742_WORKSTATION_HANDOFF.md`.

The first executable task is to verify the handoff commit and continue bounded
finalization/phase admission. Do not begin another general audit or another
production release attempt while shared admission is known exhausted.

## Exact delivery state

| Surface              | Last verified state                                                                                                                                                 | What this does not establish                                            |
| -------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------- |
| Production Website   | PR99, `ee788511`, canonical run `34308171571` succeeded; migration 0101 applied. Fresh public TP53/BRCA1 profiles and portraits passed at that release.             | Later main commits are not deployed. No full-day normal-workload proof. |
| Website main         | `2a9a4b1a`, including PR100/101 status-list/index and migration-sizing work. Both subsequent release attempts refused before production mutation.                   | Migration 0102 is **not applied**.                                      |
| Website local branch | `b742-status-migration-source-sizing`; starting commit `af7fd529` repairs the blank-completion-time scan. A further scoped catalog/essence lookup patch is present. | No push, review or deployment of these local changes is claimed.        |
| Workstation          | `a28798c` backend running through canonical launcher `run-36bb9209f2d1`; `39dc756` built UI served locally.                                                         | Exact cold prose remains unavailable while cost admission is exhausted. |
| Background/Drain     | Provider Cron/Queue containment remains active. Three saved GLB1 outputs await finalization/publication.                                                            | Disabled work is containment, not successful capacity acceptance.       |

The shared operational allocation was last measured at **1,009,059 / 1,000,000
reads**, 13,574 writes and 1,809 requests at 05:00 UTC. This is the protected
operational allocation, not a claim that Cloudflare's entire provider allowance
has been exhausted. Do not increase it or refund uncertain reservations.

The local replica previously completed 779 pages and reached epoch 2/event
58,074, with 19,737 genes and 19,154 revisions. Those metadata counts do not prove
that a newly selected prose body is cached. After cleanup, authoring database
size was measured at 487,858,176 bytes: reclamation succeeded, but future growth
is still dangerously close to the database ceiling and lacks admission.

## Completed and verified work

1. **Release and authoring recovery:** repaired the exact-commit release path,
   migration admission/continuation, published-reader recovery, catalog digest
   ordering, streaming replica/bootstrap, immutable timestamp/hash validation,
   idle polling/acknowledgement amplification, and partial saved-session identity
   handling. Normal deployment succeeded; later migrations remain pending as above.
2. **Finalization foundations:** indexed bounded scoped/global queue selection,
   exact-key asset reconciliation, transactional counters, exact job-version
   claims/transitions, bounded completion batches and durable publisher handoff.
   Their measured costs are documented in `D1_READ_EXHAUSTION_PREVENTION.md`.
   These foundations do not bound the work performed inside each phase.
3. **Workstation retry/UI containment:** removed discarded whole-catalog/essence
   preflight reads; persisted shared daily cost refusals across restart before
   registration/reservation; kept unfinished predictions/spending intact; fixed
   the blank selected-reader panel. Fresh local TP53 and BRCA1 visibly show the
   allowance refusal. Authority suite: 122 passed. UI: 206 passed, production
   build and changed-file formatting passed.
4. **Latest local query work:** blank completion timestamps now cost at most two
   reads beside 80,000 historical jobs. Explicit catalog/essence scopes now use
   unique-key probes, preserve membership above 1,000 keys, and exclude unrelated
   growth. Real D1 receipts for either table: 1 key = 2 reads; 1,001 = 2,002;
   25,000 = 50,000; zero writes, unchanged beside 60,000 unrelated rows.
   The interrupted full test command continued and completed: **1,965 repository
   tests and 26 prose tests passed**. Do not describe it as an aborted pass.

At handoff, the normal Website TypeScript/format gate also passed. Subsequent
edits added documentation and source comments only; cost identities were
regenerated. The handoff commit contains the scoped-state patch and its evidence,
so the next model should inspect that commit rather than reconstruct the patch.

## Chesterton's fences and decision rationales

| Mechanism                                    | Why it exists / guarantee retained                                                                                                                | Rule for the replacement                                                                                                                                                                                                                             |
| -------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Shared cost admission and protected headroom | Several individually plausible operations exhausted one shared account. Lost responses may already have spent resources.                          | Reserve before expensive work, preserve immutable predictions and continuation lineage, include retries and authority overhead. Unknown bounds fail closed. Never manufacture capacity through new IDs, refunds, extra ledgers or larger allowances. |
| Canonical deployment and schema transition   | A new Worker against old tables can break every reader. Review/CI and migration receipts establish the executable/schema pair.                    | Exact-commit CI, canonical admitted migration/deployment, completed pipeline, fresh browser checks. Do not restart a dependent client across incompatible schema. Do not replay historical migration lineages blindly.                               |
| Published reader artifacts                   | Cold readers must not rebuild the entire catalog or write KV. Readers need a coherent published state even during recovery.                       | Preserve static-first artifact delivery and initialization before activation. A published digest cannot substitute for current D1 state when comparing unpublished changes.                                                                          |
| Durable jobs, counters and job versions      | Saved work must survive retries; old claimants cannot finish a newly enqueued version. Exact counts are user-visible truth.                       | Preserve membership, phase order, retry dates, exact counters and version checks. Do not delete history or mark unfinished work complete to lower costs.                                                                                             |
| Publisher handoff                            | Finishing per-gene phases is different from completing shared publication. The existing publisher is the sole owner.                              | Preserve durable pending handoff and its acknowledgement. Do not add a second publisher or report success from zero unfinished jobs alone.                                                                                                           |
| Affirmative reconciliation                   | Only supplied keep/legacy identities authorize changes; omitted candidates must remain untouched.                                                 | Probe exact input keys, preserve restoration/rejection precedence and explicit scoped unpublish. Do not enumerate untouched history just to count it.                                                                                                |
| Canonical votes and cold bootstrap           | Legacy D1 votes seed cold coordinators; a warm VoteCoordinator owns votes. Transactional projection replacement prevents empty partial summaries. | Bound bootstrap, export, projection and downstream phases together. Do not replace warm authority with a potentially stale D1 mirror. Swapping one helper does not certify the complete cost.                                                        |
| Authoring material privacy/provenance        | Replica metadata and plaintext bodies have different retention rules. Exact body/Tags hashes bind scientific and generation provenance.           | Keep fetched material process-only under the existing contract; do not persist plaintext or substitute old local prose to evade refused authority reads. Cleared heads stay explicitly empty; missing heads remain errors.                           |
| GLB1 saved-output identity                   | Three images already succeeded; failure recovery must not redraw them or infer a different session from global state.                             | Resume the exact session/publication, keep lease identities and durable receipts, and prove fulfillment before calling it delivered.                                                                                                                 |
| Attribution and tests                        | Aggregate ledger deltas and sampled provider queries do not identify a caller. Browser-like user agents may come from Python.                     | Require a captured caller/route plus provider evidence. Preserve uncertainty. Test focused hypotheses; do not repeatedly run every suite without a new change or unresolved concern.                                                                 |

## Remaining work — execute in this order

### 1. Verify the handoff commit and release prerequisites

Check the branch and working tree. The scoped-state implementation is
`workers/iconoplasm/sync-state-selection.js`, used by the existing runtime
hashing functions; its test is `sync-state-selection.test.js`. It preserves the
25,000-symbol contract, deduplicates keys, sorts bounded results outside D1, and
does no query for an empty normalized scope. Deliberately unscoped state reads
remain whole-state operations and are not certified by this patch.

Regenerate `workers/generated/operation-cost-identities.js` after future runtime
changes using `node scripts/generate-operation-cost-identities.mjs`. Run the
normal type/format checks and meaningful targeted tests for new edits; use the completed full
suite evidence above unless another functional edit warrants rerunning it.
Commit named files. Use protected review/CI and the normal release pipeline when
fresh shared capacity can admit the complete pending migration set. Do not
repeat known-refused deployment attempts in the meantime.

### 2. Finish bounded finalization and shared phase admission

In the main stateful runtime, locate functions by name, not stale line numbers:

- `processSyncFinalizationJobPhase`, `syncAdminReadModels` and its internal routes;
- `rebuildVoteAssetSummaryForSymbols`: still rebuilds from legacy D1 vote history;
- `ensureBootstrapped` in VoteCoordinator: still reads all assets, votes and
  eligibility for a cold gene and writes the complete local object state;
- `autoPromoteTopVotedPortrait`, gene/vision/emulsion rollups and canonical
  projection export/replacement: still have growth-sensitive work;
- the `doFreeTierExhausted` recovery exception in `syncAdminReadModels`: review
  actual wrapped behavior and retire the exception with shared admission. It is
  not an accepted reason to spend through an exhausted protection mechanism.

Design a resumable, version-safe bound for the complete phase, including D1,
Durable Object work, triggers and retries. Reuse the existing cost authority.
Prove cost under large same-gene history as well as unrelated growth; a limit on
returned rows or a small fixture is insufficient. Preserve canonical ranking,
caretaker eligibility, one publisher and exact completion semantics.

### 3. Account for the remaining expensive callers and complete growth bounds

Provider Query Insights observed full catalog reads of 19,023 rows and essence
reads of 19,737 rows. The discarded preflight copies were removed, but attribution
of all remaining full reads is still incomplete. Local admin transport now logs
only route, scope count and caller function names for such requests. No new
global-read log appeared in the last observed running app. Chrome/137 is that
Python transport's configured user agent, not proof of another browser.

The 25 Python publication/catalog tests passed with external connections denied
and zero attempts. A separate 1,964-test Node run also passed with external
HTTP/socket traffic denied. Neither test run proves every possible caller is safe.

Finish the other audit obligations: personal shelf/encounter writes, inbox
acknowledgement and source fanout, normal catalog publication, every scheduled/
queue/operator/raw-binding entrypoint, authoring physical storage growth, and
the cost authority's own resource use. Keep the audit inventory current; do not
silently classify unknown paths as free.

### 4. Release and recover the saved product outcome

Once the complete release fits fresh protected headroom, apply all reviewed
pending migrations through normal admission, wait for activation and verify at
least two fresh relevant pages after JavaScript settles. Then restore required
background work coherently with the existing reset-recovery controller, which
otherwise reasserts containment. Do not simply toggle Queue/Cron on.

Resume session `20260906-050817Z--rax2-era-anima`, publication
`pub-0d4a64398ddff0ce842c17a1`. Three GLB1 outputs are saved; one queued reconcile
and the publisher handoff remain. Prove published assets, request fulfillment,
delivery, and a cold workstation exact body/Tags read. Do not redraw the five
failed jobs as a shortcut to handling the three successful outputs.

### 5. Complete acceptance, not just deployment

Verify normal readers, voting/discovery, caretaker/admin, generation and partial
recovery. Collect one complete UTC reset-to-reset interval with intended
background and product work enabled, protected headroom, no quota refusals and
no unexplained recurring large scans. Only then close B-742. Full tests, a
successful deployment, flat usage under containment or a daily reset alone do
not satisfy the issue.

## Evidence and execution hygiene

Evidence is under each project's `artifacts/b742-recovery-20260909/`; earlier
Website recovery evidence is under `artifacts/b742-sysop-20260908/`. Particularly
useful recent files: `scoped-state-cost-tests.log`, `scoped-state-full-tests.log`,
`query-insights-0440-0455.json`, `live-cost-trace.jsonl`; workstation
`cost-cooldown-tests.log`, `reader-error-ui-check.log`,
`python-isolated-preflight-tests.log`.

Use PowerShell 7 and `D:\Coding\scripts\Invoke-HardTimeout.ps1` for commands
that may exceed five seconds. Use pnpm with the existing 24-hour release-age
policy and the workstation's dedicated `.venv`. Discover exact filenames before
reading them. Keep evidence in project artifacts directories; never print
tokens, raw environment/configuration, browser-presence query values or private
request bodies. Preserve unknown work. Avoid broad reformatting beyond the
project's required changed-file checks.

The Codex automation `complete-b-742-recovery-and-verification` is **paused for
the owner's weekly model-budget handoff**. This does not alter production's
recovery schedules or mean B-742 is resolved. Resume the Codex automation only
when the owner wants automatic model spending again. Update Linear at meaningful
boundaries and before compaction; link this document instead of duplicating an
ever-growing incident narrative.
